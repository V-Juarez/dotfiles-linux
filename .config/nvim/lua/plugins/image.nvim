return {
  "3rd/image.nvim",
  config = function()
    require("image").setup({
      backend = "kitty",
      max_width = 10,
      max_height = 10,
      max_width_window_percentage = 10,
      max_height_window_percentage = 10,
    })
  end,
}
