--[[
Rings animados para Conky
~/.config/conky/lua-rings/rings.lua
]]

-- Intentar cargar cairo de diferentes formas
local cairo_loaded = false
local cairo

-- Método 1: Usar lgi (recomendado)
local status, lgi = pcall(require, 'lgi')
if status then
    cairo = lgi.cairo
    cairo_loaded = true
else
    -- Método 2: Intentar cargar cairo directamente (cairo-lua)
    status = pcall(require, 'cairo')
    if status then
        cairo_loaded = true
    end
end

if not cairo_loaded then
    print("ERROR: No se pudo cargar cairo. Instala lua-lgi: sudo pacman -S lua-lgi")
    return
end

-- Configuración de los rings (ajustados para 24")
settings_table = {
    {
        -- Ring de CPU
        name='cpu',
        arg='cpu0',
        max=100,
        bg_colour=0x1e1e2e,
        bg_alpha=0.4,
        fg_colour=0x89b4fa,
        fg_alpha=0.8,
        x=110, y=120,
        radius=45,
        thickness=8,
        start_angle=-90,
        end_angle=270
    },
    {
        -- Ring de RAM
        name='memperc',
        arg='',
        max=100,
        bg_colour=0x1e1e2e,
        bg_alpha=0.4,
        fg_colour=0xa6e3a1,
        fg_alpha=0.8,
        x=110, y=260,
        radius=45,
        thickness=8,
        start_angle=-90,
        end_angle=270
    },
    {
        -- Ring de Disco
        name='fs_used_perc',
        arg='/',
        max=100,
        bg_colour=0x1e1e2e,
        bg_alpha=0.4,
        fg_colour=0xf38ba8,
        fg_alpha=0.8,
        x=110, y=400,
        radius=45,
        thickness=8,
        start_angle=-90,
        end_angle=270
    },
    {
        -- Ring de Red Download
        name='downspeedf',
        arg='enp0s25',
        max=100,
        bg_colour=0x1e1e2e,
        bg_alpha=0.4,
        fg_colour=0xf9e2af,
        fg_alpha=0.8,
        x=110, y=540,
        radius=45,
        thickness=8,
        start_angle=-90,
        end_angle=270
    },
}

-- Función para dibujar un ring
function draw_ring(cr, t, pt)
    local w, h = conky_window.width, conky_window.height
    
    local xc, yc, ring_r, ring_w, sa, ea = pt['x'], pt['y'], pt['radius'], pt['thickness'], pt['start_angle'], pt['end_angle']
    local bgc, bga, fgc, fga = pt['bg_colour'], pt['bg_alpha'], pt['fg_colour'], pt['fg_alpha']

    local angle_0 = sa * (2 * math.pi / 360) - math.pi / 2
    local angle_f = ea * (2 * math.pi / 360) - math.pi / 2
    local t_arc = t * (angle_f - angle_0)

    -- Background ring
    cairo_arc(cr, xc, yc, ring_r, angle_0, angle_f)
    cairo_set_source_rgba(cr, rgb_to_r_g_b(bgc, bga))
    cairo_set_line_width(cr, ring_w)
    cairo_stroke(cr)
    
    -- Foreground ring
    cairo_arc(cr, xc, yc, ring_r, angle_0, angle_0 + t_arc)
    cairo_set_source_rgba(cr, rgb_to_r_g_b(fgc, fga))
    cairo_stroke(cr)
    
    -- Glow effect
    cairo_arc(cr, xc, yc, ring_r, angle_0, angle_0 + t_arc)
    cairo_set_source_rgba(cr, rgb_to_r_g_b(fgc, fga * 0.3))
    cairo_set_line_width(cr, ring_w + 4)
    cairo_stroke(cr)
end

-- Convertir color hex a RGB
function rgb_to_r_g_b(colour, alpha)
    return ((colour / 0x10000) % 0x100) / 255., ((colour / 0x100) % 0x100) / 255., (colour % 0x100) / 255., alpha
end

-- Función principal
function conky_rings()
    local function setup_rings(cr, pt)
        local str = ''
        local value = 0
        
        str = string.format('${%s %s}', pt['name'], pt['arg'])
        str = conky_parse(str)
        
        value = tonumber(str)
        if value == nil then value = 0 end
        
        local pct = value / pt['max']
        
        draw_ring(cr, pct, pt)
    end
    
    -- Verificar que conky esté corriendo
    if conky_window == nil then return end
    
    local cs = cairo_xlib_surface_create(conky_window.display, conky_window.drawable, conky_window.visual, conky_window.width, conky_window.height)
    local cr = cairo_create(cs)
    
    local updates = conky_parse('${updates}')
    update_num = tonumber(updates)
    
    if update_num > 5 then
        for i in pairs(settings_table) do
            setup_rings(cr, settings_table[i])
        end
    end
    
    cairo_destroy(cr)
    cairo_surface_destroy(cs)
    cr = nil
end
